version: 2

before:
  hooks:
    # Tidy all modules
    - go mod tidy
    - cd apps/cli && go mod tidy
    - cd apps/ui && go mod tidy
    # Test all modules
    - go test ./...
    - cd apps/cli && go test ./...
    - cd apps/ui && go test ./...

builds:
  - id: cli
    main: ./apps/cli
    binary: pics
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.version={{.Version}}

archives:
  - id: cli-archives
    name_template: >-
      {{ .ProjectName }}-cli_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    files:
      - README.md
      - LICENSE*

checksum:
  name_template: 'checksums.txt'

changelog:
  sort: asc
  use: github
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - Merge pull request
      - Merge remote-tracking branch
      - Merge branch

release:
  github:
    owner: acm19
    name: pics
  draft: false
  prerelease: auto
  mode: replace
  replace_existing_artifacts: true
  header: |
    ## Installation

    ### CLI (pics)
    Lightweight command-line tool.

    **Linux/macOS:**
    ```bash
    tar -xzf pics-cli_*.tar.gz
    chmod +x pics
    sudo mv pics /usr/local/bin/
    pics --version
    ```

    **Windows:**
    Extract `pics-cli_Windows_*.zip` and add `pics.exe` to your PATH.

    ### Desktop UI (pics-ui)
    Full-featured desktop application with bundled dependencies.

    **Linux:**
    ```bash
    tar -xzf pics-ui_Linux_x86_64.tar.gz
    chmod +x pics-ui
    ./pics-ui
    ```

    **macOS:**
    Extract `pics-ui_macOS_universal.zip` and drag `pics-ui.app` to Applications.

    **Windows:**
    Extract `pics-ui_Windows_x86_64.zip` and run `pics-ui.exe`.

    ### Dependencies (CLI only)
    - **ExifTool**: Required for EXIF data extraction.
    - **jpegoptim**: Required for JPEG compression (optional).

  footer: |
    **Full Changelog**: https://github.com/acm19/pics/compare/{{ .PreviousTag }}...{{ .Tag }}
